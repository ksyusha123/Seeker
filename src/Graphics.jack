class Graphics {
    function void erase(int memAddress, int rowsCount) {
        var int i;
        while (i < rowsCount) {
            do Memory.poke(memAddress + (i * 32), 0);
            let i = i + 1;
        }
        return;
    }
}