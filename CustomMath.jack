class CustomMath {
    function int remainder(int number, int divisor) {
        return (number - (divisor * (number / divisor)));
    }

    // function Array convertToBinary(int number) {

    // }
}